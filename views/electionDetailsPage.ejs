<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title><%=title%></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="grid grid-cols-6 pt-5">
        <div class="col-start-2 col-span-2">
            <h1 class="text-2xl font-semibold text-gray-600"><%=election.title%></h1>
      <div>
       <div>
       <% if(questions.length==0){%>
          <p>You haven't created any questions</p>
        <%}%>
        <% if(questions.length > 0){%>
          <p>You have created <%=questions.length%> questions</p>
          <%}%>
       </div>
       <div>
        <%for(var i=0;i<questions.length;i++){%>
          <a href="/questions/<%=questions[i].id%>" class="text-vlue-600"><%=questions[i].title%></a>
          <%}%>
       </div>
       <a href="/elections/<%=election.id%>/questions" class="text-green-600">Manage questions</a>

      </div>
      </div>
    </div>
  </body>
</html>

