<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="csrf-token" content="<%= csrfToken %>" />
  <title>
    <%=title%>
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
  body {
    font-family: 'Jost', sans-serif;

  }
</style>

<body>
  <main class=" h-screen" style="background-color: #6c6c7f24; ">
     <%-include('header.ejs',{admin:admin})%> 
      <div class="  sm:container mx-auto p-10 bg-white shadow-lg rounded-md "
        style=" margin: 30px auto; height: 75%; width: 65%;">
        <div class="mx-auto">
          <% var data=[ {label:"Home",url:"/elections",current:false}, {label:"Create new election",url:"/elections/createElections/new",current:true} ] %>
            <%- include('navigation.ejs',{data:data})%>
            <div class="p-8 mx-auto flex flex-col align-center justify-center container" style="width: 500px;">
              <h1 class="text-gray-600 text-2xl font-semibold text-center">Create new election</h1>
              <form method="post" action="/elections/createElection" onsubmit="submit_btn.disabled = true; return true;">
                <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
                <div class="p-3">
                  <label for="title" class="text-gray-900 text-lg py-2">What's your election's name?</label>
                  <input type="text" name="title"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full mt-2" />
                </div>
                <div class="p-3">
                  <label for="url" class="text-gray-900 text-lg py-2">What's the custom string for the
                    election?</label>
                  <input type="text" name="url"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full mt-2" />
                </div>
                <div class="p-3">
                  <button type="submit" name="submit_btn"
                    class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2 w-full">SUBMIT</button>
                </div>
              </form>
              <%-include("errorMsg.ejs")%> 

        </div>
      </div>

  </main>
</body>

</html>