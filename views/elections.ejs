<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title><%=title%></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="grid grid-cols-6 pt-5">
        <div class="col-start-2 col-span-2">

      <div>
        <p class="py-2">Welcome <%=admin.firstName%> <%=admin.lastName%></p>
        <div>
          <%if(elections.length == 0){%>
            <p>You haven't created any elections</p>
            <%}%>
            <p>You have created <%=elections.length%> elections</p>
        </div>
        <div>
          <%for(var i=0;i<elections.length;i++){%>
            <a href="/elections/<%=elections[i].id%>" class="text-vlue-600 block"><%=elections[i].title%></a>
            <%}%>
            
        </div>
        <a href="/elections/new" class="text-green-600">Create new election</a>
      </div>
      </div>
    </div>
  </body>
</html>
