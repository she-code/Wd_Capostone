<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title><%=title%></title>
    <script src="https://cdn.tailwindcss.com"></script>
   
  </head>
  <body>
    <div class="grid grid-cols-6 pt-5">
        <div class="col-start-2 col-span-2">
          <%
          var data=[
      {label:"Home",url:"/elections",current:true}
      ]
          %>
          <%- include('navigation.ejs',{data:data})%>
          <a href="/signout" class="text-green-600">Sign Out</a>
      <div>
        <p class="py-2 text-xl font-bold text-gray-500">Welcome <%=admin.firstName%> <%=admin.lastName%></p>
        <div>
          <%if(elections.length == 0){%>
            <p>You haven't created any elections</p>
            <%}%>
            <%if(elections.length > 0){%>
              <p>You have created <%=elections.length%> elections</p>
              <%}%>
          
        </div>
        <ul class="list-disc">
          <%for(var i=0;i<elections.length;i++){%>
            <li class="ml-5"><a href="/elections/<%=elections[i].id%>" class="text-blue-600 block"><%=elections[i].title%></a></li>
            <%}%>
            
        </ul>
        <a href="/elections/new" class="text-green-600">Create new election</a>
      </div>
      </div>
    </div>
  </body>
</html>
